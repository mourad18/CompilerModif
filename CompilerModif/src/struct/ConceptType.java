package struct;
/**
 * 
 * @author root
 * The concept type is the placement of the concept in the hasse diagram.
 * only one concept can have TOP value. And only one concept can have BOTTOM value.
 * If our work, we analysis the concepts with INNER.
 */
public enum ConceptType {
	
	TOP,
	BOTTOM,
	INNER;
	
}
