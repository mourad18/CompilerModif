package struct;

/**
 * 
 * @author root
 * Each table may support one of the following search type :
 * EXACT.
 * LPM.
 * A field is also associated with a search type.
 */
public enum MatchType {
	EXACT,
	LPM;
}
